---
phase: 08-demo-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .gitignore
  - vercel.json
  - sdk/app/components/Layout.tsx
autonomous: false

must_haves:
  truths:
    - "README.md exists at repo root with project overview, Mermaid architecture diagram, verified setup commands, deployed contract addresses with Starkscan links, tech stack table, and demo section"
    - ".gitignore excludes sdk/node_modules/, sdk/dist/, and planning PDFs from version control"
    - "vercel.json exists at repo root with COOP/COEP headers (credentialless) and correct build config pointing to sdk/"
    - "Layout.tsx GitHub link points to the actual repository URL (not the anthropics placeholder)"
  artifacts:
    - path: "README.md"
      provides: "Hackathon-judge-optimized project documentation"
      contains: "mermaid"
    - path: ".gitignore"
      provides: "Clean git tree excluding build artifacts"
      contains: "sdk/node_modules"
    - path: "vercel.json"
      provides: "Vercel deployment config with cross-origin isolation headers"
      contains: "Cross-Origin-Embedder-Policy"
    - path: "sdk/app/components/Layout.tsx"
      provides: "Footer with correct GitHub link"
  key_links:
    - from: "README.md"
      to: "deployments.json"
      via: "Contract addresses referenced in README match deployments.json"
      pattern: "0x054ca264|0x9afed88|0x483b48c"
    - from: "vercel.json"
      to: "sdk/vite.config.ts"
      via: "COEP value matches (credentialless, not require-corp)"
      pattern: "credentialless"
---

<objective>
Create the README.md, vercel.json, fix .gitignore, and update the Layout.tsx GitHub placeholder -- all the artifacts needed for a polished hackathon submission.

Purpose: Judges evaluate documentation quality, live deployment, and GitHub repo first. This plan creates every artifact Claude can automate so the user only needs to handle human-only tasks (recording, uploading, form submission).
Output: README.md, vercel.json, updated .gitignore, fixed Layout.tsx
</objective>

<execution_context>
@/Users/laitsky/.claude/get-shit-done/workflows/execute-plan.md
@/Users/laitsky/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-demo-submission/08-RESEARCH.md
@deployments.json
@sdk/vite.config.ts
@sdk/package.json
@.gitignore
@sdk/app/components/Layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md, vercel.json, and fix .gitignore</name>
  <files>README.md, .gitignore, vercel.json</files>
  <action>
    **1. Create README.md at repo root** with this exact structure:

    ```markdown
    # StarkShield
    > Prove who you are without revealing who you are -- privacy-preserving credential verification on Starknet.

    ## Overview
    2-3 sentences: StarkShield lets users prove attributes (age, group membership) to on-chain smart contracts without revealing underlying personal data. Uses Noir zero-knowledge circuits with Garaga-generated Cairo verifiers for fully on-chain, trust-minimized verification. Built for the Starknet Re{define} Hackathon -- Privacy Track.

    ## Architecture
    Include the verified Mermaid diagram from 08-RESEARCH.md (the graph TD with Client, Wallet, Chain, dApp subgraphs). Use the exact diagram from research -- it has been verified to render on GitHub.

    ## How It Works
    Numbered steps:
    1. **Credential Issuance** -- A trusted issuer signs a credential (age, membership) using Poseidon2-Schnorr signatures
    2. **Client-Side Proof** -- The user loads their credential in the browser; Noir circuit + bb.js WASM generate a ZK proof entirely client-side
    3. **Calldata Encoding** -- Garaga WASM encodes the proof into Starknet-compatible calldata
    4. **On-Chain Verification** -- The proof is submitted to StarkShieldRegistry, which routes to the Garaga verifier, checks the trusted issuer, enforces nullifier uniqueness, and logs the result
    5. **Composable Queries** -- Any downstream dApp can query verification records by nullifier -- no personal data ever touches the chain

    ## Deployed Contracts (Starknet Sepolia)
    Table with Contract name, truncated Address (backtick-formatted), and clickable Starkscan Explorer links. Use addresses from deployments.json:
    - StarkShieldRegistry: 0x054ca264033ae3b5874574c84de9c6086d94a66fb65445e455a8cef3137b7fab
    - Age Verifier: 0x9afed88f1d6bb0da51d98d29a3aaca31ed7ca99dc51a3df06931c543694f52
    - Membership Verifier: 0x483b48c3dbd32ebbc45b22a2a419c9a95c3999b103f5eb4a3048a0e8000d1da

    Also include a Verification Transactions table with tx hashes from deployments.json (Registry Deploy, Add Age Issuer, Age Verify+Register) with Starkscan links.

    ## Tech Stack
    Table with Layer and Technology columns:
    - Circuits: Noir 1.0.0-beta.16 (Aztec)
    - Proof System: UltraKeccakZK Honk via bb.js
    - Verifier Generation: Garaga SDK 1.0.1
    - Smart Contracts: Cairo / Scarb 2.14.0
    - On-Chain: Starknet Sepolia
    - SDK: TypeScript + noir_js + bb.js WASM
    - Frontend: React 19 + Vite 6 + Tailwind CSS 4
    - Wallet: starknet.js v8 + get-starknet v4

    ## Quick Start
    Prerequisites table (Node.js 20+, Noir 1.0.0-beta.16 via noirup, Scarb 2.14.0 via asdf, Python 3.10.x for Garaga, Garaga 1.0.1 via pip). Mention .tool-versions + mise install shortcut.
    Then build commands:
    ```bash
    git clone <REPO_URL>
    cd starkshield

    # Compile circuits
    cd circuits && nargo compile && cd ..

    # Build contracts (already deployed -- optional)
    cd contracts && scarb build && cd ..

    # Run web app
    cd sdk && npm install && npm run dev
    ```

    ## Demo
    Placeholder sections for:
    - Demo Video: `[Demo Video](VIDEO_URL)` (user fills in after recording)
    - Live App: `[Live App](VERCEL_URL)` (user fills in after deployment)

    ## Project Structure
    Concise tree showing circuits/, contracts/, sdk/, scripts/, deployments.json

    ## Privacy Guarantees
    Bullet points: credentials never leave device, proof generation is client-side WASM, only nullifiers and public outputs go on-chain, per-dApp nullifiers prevent cross-dApp tracking

    ## Development
    Brief section: `npm run dev` in sdk/ for local dev, `nargo test` for circuit tests, `scarb build` for contracts

    ## License
    MIT
    ```

    **2. Update .gitignore** -- Append these entries at the end:
    ```
    # SDK build artifacts
    sdk/node_modules/
    sdk/dist/

    # Planning documents (not for public repo)
    .planning/
    StarkShield_PRD.pdf
    StarkShield_Implementation_Plan.pdf
    ```

    **3. Create vercel.json at repo root** with this exact content:
    ```json
    {
      "buildCommand": "cd sdk && npm install && npm run build",
      "outputDirectory": "sdk/dist",
      "headers": [
        {
          "source": "/(.*)",
          "headers": [
            {
              "key": "Cross-Origin-Embedder-Policy",
              "value": "credentialless"
            },
            {
              "key": "Cross-Origin-Opener-Policy",
              "value": "same-origin"
            }
          ]
        }
      ]
    }
    ```

    IMPORTANT: Use "credentialless" for COEP (not "require-corp") -- this matches vite.config.ts and avoids breaking wallet extension modals.
  </action>
  <verify>
    - `cat README.md | head -5` shows title and tagline
    - `grep -c "mermaid" README.md` returns at least 1 (diagram present)
    - `grep "0x054ca264" README.md` finds registry address
    - `grep "0x9afed88" README.md` finds age verifier address
    - `grep "sdk/node_modules" .gitignore` confirms entry
    - `grep "sdk/dist" .gitignore` confirms entry
    - `cat vercel.json | grep "credentialless"` confirms correct COEP value
    - `cat vercel.json | grep "sdk/dist"` confirms output directory
  </verify>
  <done>
    README.md exists with Mermaid diagram, contract addresses matching deployments.json, setup commands, and tech stack table. .gitignore blocks sdk/node_modules and sdk/dist. vercel.json has COOP/COEP headers with credentialless policy and correct build config.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Get GitHub repository URL for Layout.tsx and README</name>
  <files>sdk/app/components/Layout.tsx, README.md</files>
  <action>
    Pause and ask the user for their GitHub repository URL. The Layout.tsx footer currently links to `https://github.com/anthropics` (placeholder). No git remote is configured. The user must create a public GitHub repo and provide the URL.
  </action>
  <verify>User provides a GitHub URL or explicitly chooses to skip</verify>
  <done>GitHub URL obtained (or user chose to skip and will update manually later)</done>
  <decision>What is the GitHub repository URL for StarkShield?</decision>
  <context>
    The Layout.tsx footer currently links to `https://github.com/anthropics` (placeholder from Phase 7). The README.md has `REPO_URL` placeholder in the git clone command. Both need the real URL before deployment and submission.

    No git remote is currently configured (`git remote -v` returns empty).

    The user needs to:
    1. Create the GitHub repo (if not already done)
    2. Ensure it is PUBLIC (DoraHacks requires public repo)
    3. Provide the URL (e.g., https://github.com/laitsky/starkshield)
  </context>
  <options>
    <option id="provide-url">
      <name>Provide GitHub URL</name>
      <pros>Layout.tsx and README updated immediately</pros>
      <cons>None</cons>
    </option>
    <option id="skip-for-now">
      <name>Skip -- will update manually later</name>
      <pros>Can proceed without GitHub setup</pros>
      <cons>Must remember to update 2 files before submission</cons>
    </option>
  </options>
  <resume-signal>Provide the GitHub URL (e.g., https://github.com/username/starkshield) or type "skip"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update Layout.tsx GitHub link and README repo URL</name>
  <files>sdk/app/components/Layout.tsx, README.md</files>
  <action>
    Based on the user's response from Task 2:

    **If URL provided:**
    1. In `sdk/app/components/Layout.tsx`, replace `https://github.com/anthropics` with the provided GitHub URL on line 107
    2. In `README.md`, replace `<REPO_URL>` in the git clone command with the provided URL

    **If "skip":**
    1. In `sdk/app/components/Layout.tsx`, replace `https://github.com/anthropics` with `https://github.com/TODO-UPDATE` to make the placeholder more obvious
    2. Leave README.md clone URL as `<REPO_URL>` -- it is already clearly a placeholder
  </action>
  <verify>
    - `grep "anthropics" sdk/app/components/Layout.tsx` returns NO matches (placeholder removed)
    - `grep "github.com" sdk/app/components/Layout.tsx` shows the updated URL
    - `grep "git clone" README.md` shows the correct clone command
  </verify>
  <done>
    Layout.tsx footer links to actual GitHub repo (or explicit TODO placeholder). README clone command has matching URL.
  </done>
</task>

</tasks>

<verification>
1. `ls README.md vercel.json` -- both files exist at repo root
2. `grep "mermaid" README.md` -- architecture diagram present
3. `grep "credentialless" vercel.json` -- correct COEP policy
4. `grep "sdk/node_modules" .gitignore` -- gitignore updated
5. `grep -c "anthropics" sdk/app/components/Layout.tsx` -- returns 0 (placeholder removed)
6. All three contract addresses from deployments.json appear in README.md with clickable Starkscan links
</verification>

<success_criteria>
- README.md is judge-ready: overview, Mermaid diagram, contract addresses, tech stack, setup commands
- vercel.json enables Vercel deployment with SharedArrayBuffer support
- .gitignore prevents sdk/node_modules, sdk/dist, and planning docs from being committed
- Layout.tsx GitHub link is updated (or explicitly marked as TODO)
- All contract addresses match deployments.json exactly
</success_criteria>

<output>
After completion, create `.planning/phases/08-demo-submission/08-01-SUMMARY.md`
</output>
